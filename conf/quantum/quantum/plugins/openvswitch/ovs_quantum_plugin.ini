[DATABASE]
# This line MUST be changed to actually run the plugin.
# Example: sql_connection = mysql://root:engine@127.0.0.1:3336/ovs_quantum
sql_connection = sqlite://

[OVS]
integration-bridge = br-int
